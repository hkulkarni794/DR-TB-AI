# DR-TB-AI
This resource contains, codes, necessary arrays and pretrained AI models to predict drug resistant tuberculosis from chest X-ray images.
Provided here are the Jupyter Notebooks that predict drug resistant tuberculosis, the numpy arrays that the ensemble model uses and the component models that make up the ensemble model.
